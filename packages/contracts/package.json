{
  "name": "@shapeshiftoss/contracts",
  "version": "1.0.1",
  "packageManager": "yarn@3.5.0",
  "repository": "https://github.com/shapeshift/web",
  "license": "MIT",
  "main": "dist-cjs/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist-cjs/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "dist-cjs"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "yarn clean && cp src/env.esm.ts src/env.ts && yarn run -T tsc --build && cp src/env.cjs.ts src/env.ts && yarn run -T tsc --build tsconfig.cjs.json && cp src/env.esm.ts src/env.ts",
    "build:cjs": "yarn run -T tsc --build tsconfig.cjs.json",
    "clean": "rm -rf dist dist-cjs && rm -f tsconfig*.tsbuildinfo 2>/dev/null || true",
    "dev": "yarn run -T tsc --build --watch"
  },
  "dependencies": {
    "@shapeshiftoss/caip": "workspace:^",
    "@shapeshiftoss/types": "workspace:^",
    "@shapeshiftoss/utils": "workspace:^"
  }
}

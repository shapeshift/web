# Railway snapshot ignore - reduce repo snapshot size for Railway builds
# NOTE: Railway does NOT support gitignore root-scoping (/src/ vs src/)
# so we avoid patterns that match at every directory level.

# Development tooling
.github/
.cursor/
.claude/
.agents/
prds/
scripts/
headers/
__mocks__/
e2e/

# Documentation
docs/

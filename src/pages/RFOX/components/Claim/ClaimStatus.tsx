import type { FC } from 'react'

import type { ClaimRouteProps } from './types'

type ClaimStatusProps = {
  txId: string | undefined
}

export const ClaimStatus: FC<ClaimRouteProps & ClaimStatusProps> = () => {
  return <></>
}
